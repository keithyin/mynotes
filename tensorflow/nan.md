# tensorflow nan
在使用`tensorflow`时候碰到`nan`是一件很头疼的事情，但是如果你知道问题出在哪，那就很容易解决了。

1. 检查你的数据集有没有问题，如果数据集某一项数据为`nan`，那么你将会在处理的时候碰到`nan`
2. 0*log(0)，会导致`nan`，这个函数一般是用在`cross entropy`中，这是你可以使用`0*log(0+ 1e-10)`来解决这个问题
3. `relu`后接`softmax`时，由于`relu`输出的值可能会很大，所以会出现`1/inf`这种问题，这是考虑使用`relu6`
