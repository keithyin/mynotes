
# Vector
```rust
let values = vec!["Hello".to_string(), "world".to_string()];

let mut values = vec!["Hello".to_string(), "world".to_string()];
values.push("haha".to_string());

// 遍历

values.iter().map() // .iter遍历的是引用
value.into_iter().map() // .into_iter() 会发生所有权的转移

// for .. in .. 调用的是 into_iter
for v in values {

}

```
# HashMap


# HashSet



