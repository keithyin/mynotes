# cargo 使用

* 创建工程: `cargo new projname`
* 编译代码: `cargo build`
* 语法检查: `cargo check`
* 运行 `cargo run` (编译之后才能操作？)

```rust
fn main() {
    // 打印函数的 宏
    println!("hello world!");
}
```



# 变量

